__author__ = 'augusto'

import numpy as np


def cost_not_regularized(self, X, y):
    """
    Return the cost function value for a training set
    Training set will have N samples of M features
    Output of NN is vector of K values
    X is np.array of X.shape (M, N)
    y is np.array of y.shape (K, N)
    """
    #TODO: Think of vectorized way of obtaining cost function

    return np.float64(3)
